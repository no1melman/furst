# C++ Features Learnt

Features listed here are understood and won't be explained again.

- `#pragma once` — include guard, prevents duplicate processing
- `namespace` — declaration, reopening, closing with `// namespace` comment, `::` scope resolution
